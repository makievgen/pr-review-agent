# Copy to .env for local testing only. Never commit the real .env.
# In GitHub Actions these are read from repo Secrets instead (Settings -> Secrets and variables -> Actions).

GROQ_API_KEY=
GITHUB_TOKEN=
PR_NUMBER=
REPO=owner/repo
